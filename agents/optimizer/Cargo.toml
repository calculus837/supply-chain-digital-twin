[package]
name = "optimizer"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
good_lp = { version = "1", default-features = false, features = ["minilp"] }
